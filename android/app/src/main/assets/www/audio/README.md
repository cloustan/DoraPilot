Place Dora UI audio assets here.

Expected optional files:

- `dora_chime.wav` - preferred full-quality assistant chime used when voice responses are disabled.
- `dora_chime.mp3` - fallback compressed assistant chime.

If both files are missing, the WebView falls back to a tiny generated WebAudio chime.
